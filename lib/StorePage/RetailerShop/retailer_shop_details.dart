import 'package:flutter/material.dart';

class RetailerShop extends StatelessWidget {
  const RetailerShop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 113,
    );
  }
}
